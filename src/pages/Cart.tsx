import React from "react";
import CartSection from "../features/cart/CartSection";

const Cart: React.FC = () => {
	return (
		<div className="w-full h-full">
			<CartSection />
		</div>
	);
};

export default Cart;
